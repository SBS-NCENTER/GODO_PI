# PROGRESS.md — 2026-W19 (2026-05-04 → 2026-05-10 KST)

Archived weekly. Master: [../PROGRESS.md](../PROGRESS.md).

---

### 2026-05-07 → 2026-05-08 (evening + morning — 17:00 KST → 12:00 KST, thirtieth-session — issue#37 K=3 consecutive-misses gate + issue#36 yaw tripwire elimination ship through full pipeline as PR #107 squash; Round A `amcl.range_min_m` HIL sweep [0.15 / 4 / 4.5 / 5 / 7 m] determines 4.5 m as production sweet spot; Bug B "5 cm widening" structurally eliminated via near-LiDAR fixture filtering; pose_x σ 7.7→2.44 mm (3.2× improvement); stacked-PR-base-stale pitfall caught at session-close, PR #110 re-targeted)

Opened directly off twenty-ninth-session close (main = `1af71ee` after PR #105 chronicler output, then PR #106 chore-memory at `0928441`). Operator brief at session-open: "이제 다음 세션 이어서 진행하자~~ 아까 했던 작업에서 더 테스트를 해보는게 좋을 것 같아서 GODO_PHASE0=1 제거는 아직 안했어 / 현재 상태 확인 및 git 상태 확인하고 마저 이어 진행하자". Session ran TWO major shipping rounds + an algorithmic-stability characterization round end-to-end:
1. **issue#37 ParallelEvalPool K=3 consecutive-misses gate** through full pipeline twice (Plan R1 → Mode-A R1 → operator pivot from c1 deadline raise to c2 K-gate → Plan R2 → Mode-A R2 → Writer → Mode-B → operator HIL ≥ 6 h on news-pi01).
2. **issue#36 yaw tripwire full elimination** stacked on issue#37 (Plan → Mode-A → Writer → Mode-B → squash-merged into #107 via #108).
3. **Round A `amcl.range_min_m` sweep** — Bug B characterization HIL on news-pi01 across 5 settings (0.15 / 4 / 4.5 / 5 / 7 m), 4.5 m chosen as production setting (53-min validation, 31,668 samples, Bug B 50+ mm = 0).

Total: 1 PR merged into main (#107 — squash includes both issue#37 and issue#36), 1 PR queued for merge (#110 — re-target after stacked-PR-stale pitfall), 1 docs PR (this chronicler output, #111) to follow. Wall-clock ~16 hours with sleep included (overnight HIL runs unattended).

**Notable structural revelation #1 — Pool fate-coupling longevity weakness shipped as issue#37**. Long-run HIL on `7a91806` (PR #104 issue#19 + PR #99 issue#11 stack) measured **1 `[pool-degraded]` event in 6 h 12 min** which permanently flipped the `Amcl::pool()` single-instance into inline-sequential mode, simultaneously extinguishing issue#11 fork-join lift (~2.25×) AND issue#19 EDT 2D parallelization lift (~1.43×) for the remaining 4 h 19 min (62 % of run). Cost: pose_x σ 27.7 → 41.1 mm (+48 %); evaluate_scan p50 42.3 → 96.9 ms (+129 %); cold-path Hz 9.99 → 7.06 (-29 %). Operator framing 2026-05-07 KST: "Linux(Raspberry Pi OS)와 같은 Non-RTOS 환경에서는 6시간 동안 단 한 번의 50ms 스케줄링 지터가 발생하는 것은 수학적 필연에 가깝습니다. 이를 '1-Strike Out' 방식으로 처리하여 전체 풀을 영구 정지시키는 현재의 정책은 환경에 비해 너무 엄격(Brittle)합니다." Plan round-1 recommendation (c1 deadline raise 50 → 80 ms + d doc-only fallback) was Mode-A APPROVED but **operator REJECTED on technical grounds** with detailed critique of all 4 candidate options (a/b/c/d), forcing Plan round-2 to (c2) consecutive-misses gate K=3 with reset on success-completion. Final architecture: `kConsecutiveMissesGate = 3` Tier-1 constant + `Impl::consecutive_misses_` atomic counter, single-writer via existing `in_dispatch_` CAS; `[pool-miss-streak]` (K-1 absorbed) vs `[pool-degraded]` (K-th trip) log-line shapes; `fallback_count_++` relocated INSIDE the K-th trip branch. 50 ms range-proportional join deadline UNCHANGED (no `kJoinTimeoutBaseNs` value flip — round-1 c1 path completely abandoned). HIL acceptance ≥ 6 h Live mode, **0 pool-degraded events** ★ (validated 7.96 h × 197K samples). Production cold-path Hz: 7.34 (post-issue#11 baseline) → ~10 Hz steady-state (LiDAR-rate matched).

**Notable structural revelation #2 — Bug B "5cm widening" characterized as multi-basin particle filter degeneracy + cured by hard near-range cutoff**. Operator long-reported intermittent ~5 cm pose wobble during Live mode (sticky once started, x AND y simultaneous, restart-resolved). Round A 8 h baseline at default `amcl.range_min_m = 0.15 m` quantified the pattern: 6,803 events (3.4 %) in 30-50 mm Δ`pose_x` band ≈ 14/min; 75 events (0.04 %) in 50+ mm band ≈ 0.16/min (~1 per 6.4 min, matching operator report). pose_y range over 8 h: span 23 mm with max|dev| 13.3 mm. **Operator's structural hypothesis**: long flat wall on -y axis at perpendicular distance ~2.5 m + near-LiDAR fixtures (people, soft surfaces, LiDAR housing reflections) → AMCL likelihood becomes bimodal in x → weighted-mean pose oscillates between basin centroids → both x and y shift simultaneously (basins separated in (x, y) plane) → degenerate resampling kills cloud diversity → filter sticky in losing basin until process restart. Empirical sweep across `range_min_m` settings:

| range_min_m | n | duration | xy_std mean | pose_x σ | pose_y σ | yaw_std mean | Bug B 30-50 | Bug B 50+ | iters median | RT jitter max |
|---|---|---|---|---|---|---|---|---|---|---|
| 0.15 m (default) | 197 K | 8 h | 6.96 mm | **7.7 mm** | 2.2 mm | 0.100° | **3.4 %** | 0.04 % (75) | 16 (96 %) | (n/a) |
| 4 m | 7 K | 10 min | 9.24 mm | 2.71 mm | 2.88 mm | 0.109° | 0.014 % | 0.014 % (1 × 175 mm) | 17 | 26.6 µs |
| **4.5 m ★** | **32 K** | **53 min** | 9.34 mm | **2.44 mm** | **2.38 mm** | 0.113° | 0.0063 % | **0** | 17-18 | 28.8 µs |
| 5 m | 21 K | 32 min | 9.52 mm | 2.36 mm | 2.56 mm | 0.116° | 0 | 0 | 18-19 | 37.0 µs |
| 7 m | 6 K | 5 min | 10.30 mm | 1.89 mm | 2.48 mm | 0.136° | 0 | 0 | 23 (97 %, cap-fixed) | 32.4 µs |

**Counterintuitive but reproducible**: increasing `range_min_m` makes AMCL self-uncertainty (`xy_std_m`) WORSE (6.96 → 9.34 mm at 4.5 m, +34 %) BUT makes published pose value MORE stable (pose_x σ 7.7 → 2.44 mm, 3.2× better). Mechanism: fewer beams = wider posterior (AMCL honest about reduced data) BUT remaining beams more informative per-beam (less noise injection from near-LiDAR fixtures). For Live mode UE rendering — which consumes `pose_x_m`/`pose_y_m`, NOT `xy_std_m` — strict net improvement. **Operator decision (2026-05-08 KST)**: production `tracker.toml` set to `range_min_m = 4.5`. Code default UNCHANGED at 0.15 m for safe out-of-box (4.5 m is news-pi01-specific; future studios sweep their own). issue#22 (KLD-sampling adaptive N) priority preserved as Round B kickoff candidate — Bug B mitigation no longer the load-bearing motivation but algorithmic robustness + cache pressure reduction (5 m showed RT jitter max 37 µs vs 4-4.5 m's 26-29 µs due to elevated iters median 18-19) keep issue#22 valuable.

**Notable revelation #3 — yaw_std cliff at 7 m identifies -y wall oblique-beam contribution**. Sweep showed yaw_std mean monotonic increase: 0.100° (0.15 m) → 0.109° (4 m) → 0.116° (5 m) → **0.136° (7 m)** + iters cap-fixed at 23. Operator's geometric hypothesis: -y wall at perpendicular 2.5 m extends along its length to longer ranges (oblique beams at r = 3, 4, 5, 6, 7 m). At 5 m: perpendicular + r ≤ 5 m oblique gone, r > 5 m oblique still kept (yaw OK 0.116°). At 7 m: most useful oblique beams gone (only r > 7 m remain) → yaw constraint cliff. 4 / 4.5 / 5 m all preserve majority of wall data; 7 m loses most of it. Confirms operator's geometric reading + suggests **range_min_m has a studio-geometry-dependent "yaw cliff" beyond which constraint loss dominates**. Empirical anchor for future studios.

**Process violation + lesson — stacked-PR-base-stale pitfall caught at session-close**. PR #109 (Round A measurement-infra + issue#38 cap bumps + SHOTOKU Q12 doc) was opened with `--base feat/issue-37-pool-k-gate` (stacked on PR #107). When PR #107 squash-merged into main, GitHub did NOT auto-retarget #109's base to main. Operator clicked Merge on #109 ~01:23 KST 2026-05-08; PR #109's "merge commit" `672d203` landed on the orphan `feat/issue-37-pool-k-gate` branch (still alive due to stacked PR), NOT on main. The 4 commits (PHASE0 ext + cap 2→5 + cap 5→10 + SHOTOKU Q12) were NEVER reachable from main HEAD. Same root cause as PR #108 (issue#36 yaw tripwire) which had been "merged" into the same orphan branch but content reached main via PR #107's squash. PR #109's content did NOT have a parallel path — it was orphaned. Caught at chronicler Phase 0 verification when `git log --oneline 1af71ee..HEAD` showed only the issue#37 commit. **Resolution**: PR #110 opened with `--base main --head feat/round-a-prep` (same head branch, fresh re-target). Operator merges #110 + #111 (this chronicler output) at thirtieth-session close. **Lesson locked**: when a stacked PR's parent merges via squash, **immediately retarget the stacked PR's base to main BEFORE merging it**, OR verify post-merge commits land in main HEAD. Future Parent territory: capture in `.claude/memory/feedback_stacked_pr_base_retarget.md` if pattern recurs.

**Round A measurement infrastructure shipped via PR #110**. PHASE0 line in `cold_writer.cpp:phase0_emit()` extended with 4 new fields (commit `e39f266`, ~12 LOC):
- `pose_x_m` — `%.6f` (1 µm precision)
- `pose_y_m` — `%.6f`
- `xy_std_m` — `%.9g` (full mantissa for diagnostics)
- `yaw_std_deg` — `%.9g`

3 callers (OneShot, Live legacy, Live pipelined) updated to pass `result.pose.x/y` + `result.xy_std_m` + `result.yaw_std_deg`. Format precision matches `uds/json_mini.cpp` convention. ~50 bytes/scan extra ≈ 1.7 MB/h additional journal volume — within `SystemMaxUse` cap. Enables HIL post-analysis time-series extraction via `awk` pipelines (operator script `~/analyze_round_a.sh` written this session — POSIX-portable: uses `^` for exponentiation, not `**` (gawk-only), explicit multiplication `m*m` for variance computation). Same infra serves issue#22 (adaptive N) acceptance bar (sigma_xy stddev) and issue#13 (distance-weighted likelihood) sweep comparison.

**Round A enabler — issue#38 `amcl.range_min_m` validation cap bumps**. Schema row max validation 2.0 → 5.0 (commit `482689e`) → 10.0 m (commit `e28b258`, HIL-driven follow-up). Default UNCHANGED at 0.15 m. Runtime guard at `scan_ops.cpp:30` (`if (range_max_m > range_min_m)`) catches degenerate min ≥ max at runtime; range_max_m default 12.0 m so 10.0 m cap leaves 2 m safety margin. Operator can sweep 6 / 7 / 8 / 9 m via SPA Config tab without C++ rebuild. Test extended (`test_config_validate.cpp:168`) — accepts 4 / 5 / 9, rejects 10.5.

**SHOTOKU forward-kinematics override path drafted (Round C prep)**. Operator design discussion 2026-05-08 ~01:30 KST surfaced new architectural direction for issue#29 (SHOTOKU base-move + two-point cal-reset workflow): instead of trusting SHOTOKU's `(Fx, Fy, Fz)` and applying additive offset (current §2 Cases A/B/C scope), **directly compute camera world position from forward kinematics** using LiDAR-measured base `(Bx, By, ψ)` + crane geometry constants (boom length, mount offset from spec sheet) + FreeD encoder angles (cal-independent). Override SHOTOKU's `(X, Y, Z, Pan)` in FreeD packet before UDP send. Cal-independent ROOT CAUSE solution — Cases A/B/C unify into single code path. `/doc/shotoku_base_move_and_recal_design.md` updated (commit `3efbfb3`, +93 / -5 LOC):
- §3.2 Forward kinematics override path (new major section, ~70 lines): motivation + formula + Case A/B/C unification + Z handling options (z1 override / z2 passthrough) + 4 prerequisite data items (spec sheet + reference point + sign convention + toggle UX) + 3 implementation location candidates (option α cold-path / β hot-path / γ transform module) → option α recommended.
- §6 unresolved questions Q8-Q12 (NEW): override default policy, failsafe (LiDAR/AMCL down), latency align, Z passthrough vs override, **Q12 yaw override TBD pending SHOTOKU spec sheet**.
- §7 trigger conditions: Round C kickoff after Round A/B ship + spec sheet collection.

issue#29 scope SHIFTED from "re-anchor workflow" to "forward kinematics layer" (legacy Cases A/B/C marked as fall-back-only). Operator-side prerequisite: TK-53LVR / Ti-04VR spec sheet + reference point definition + encoder sign convention before Plan kickoff.

**3 PRs landed (or queued) this session**:

| PR | Issue | Title | State |
|---|---|---|---|
| #107 (`1779c02`) | issue#37 + issue#36 | feat(issue#37): ParallelEvalPool K=3 consecutive-misses gate (1-Strike → 3-Strike) — squash includes issue#36 yaw tripwire elim via stacked PR #108 | merged into main |
| #108 (`672d203`) | issue#36 | feat(issue#36): eliminate cold_writer yaw tripwire feature (full removal) | "merged" to orphan stacked branch; content reached main via #107 squash |
| #110 (open at close) | Round A + issue#38 | feat: Round A measurement-infra + issue#38 range_min cap bump + SHOTOKU Q12 doc note (re-target post-#109) | open, awaiting operator merge |

**Pipeline execution notes**:

- **issue#37 Plan round 1 → operator REJECTION → Plan round 2**. Round 1 (c1 deadline raise 50→80 ms + d doc-only) was Mode-A APPROVED with minor revisions but operator critiqued all 4 options (a) pool sep / (b) trip-recover / (c) trip relax / (d) operational on technical grounds, forcing round-2 redirect to (c2) consecutive-misses gate K=3. Lesson: **operator pivot mid-pipeline is normal — Plan rounds are not Plan acceptances**. Mode-A APPROVAL does not lock the recommendation; operator decision overrides. Round-2 re-Plan kept R1 audit findings (m1, m2, n1-n5) as carry-forward into round-2's §6 file-spec where load-bearing.
- **issue#36 stacked PR #108 squash through #107**. operator merged #108 first (squash-merge into stacked branch), then #107 to main — squash propagated into #107's diff. Net effect: both fixes ship as `1779c02` on main. PR #108 itself listed as MERGED (`672d203`) but mergeCommit lives on orphan branch lineage.
- **operator ≥ 6 h Live HIL on news-pi01** validated K=3 gate clean: 7.96 h × 197 K samples, 0 pool-degraded events, 0 pool-miss-streak events (no isolated jitter spikes occurred either — gate not exercised against real spike but no false-positive trips). pose_x σ 7.7 mm baseline (range_min=0.15 m default still) — **Bug B finding emerged in this same overnight run** as 14/min in 30-50 mm bucket and 0.16/min in 50+ mm. Round A sweep started after operator analyzed the 8 h baseline.
- **Round A iterative refinement**: operator initially tested 4 m (mistakenly entered 5 m, ran 30 min) → corrected to 4 m × 10 min → 4.5 m × 53 min ★ → 5 m × 32 min → 7 m × 5 min. Stats robust at 4.5 m + 5 m; 4 m and 7 m short. 4.5 m chosen as production sweet spot. Operator gut-feel intuition ("4.5 m로 하면 좋을 것 같은 느낌이야") preceded any data and matched empirical optimum exactly.
- **POSIX-awk portability gotcha**: initial analysis script used `**` for exponentiation (gawk-only). Pi 5 awk (mawk/POSIX) rejects with "syntax error at or near *". Fixed via `^` or explicit multiplication. Lesson: ship-portable awk uses POSIX features only.

**Open queue for next session** (priority order, operator-locked):

1. **★ issue#22 — KLD-sampling adaptive N** (Round B kickoff). Round A range_min_m=4.5 m hard cutoff eliminates Bug B but issue#22 remains valuable for: (a) OneShot first-tick efficiency (N=5000), (b) Live steady-state CPU savings → cache pressure ↓ → RT jitter ↓ further (5 m showed 37 µs jitter due to elevated iters → 4.5 m's 28.8 µs is better but issue#22 could push lower), (c) algorithmic robustness as defense against future studios where 4.5 m hard cutoff insufficient.
2. **issue#19.5** — EDT cache locality / SIMD optimisation (deferred from 29th-session TL;DR #1; range_min=4.5 m slightly relieves memory bandwidth ceiling but 1.43× → 3× target gap remains). Sequence after issue#22 since adaptive N may further reduce EDT relevance.
3. **issue#13** — distance-weighted likelihood (priority DOWN post-Round-A; hard cutoff supersedes most motivation; still valuable for heterogeneous near-feature studios).
4. **issue#26** — cross-device latency measurement tool (paused at Mode-A round 1 REJECT; Phase 5 prep work).
5. **issue#29** — SHOTOKU forward kinematics override (spec at `/doc/shotoku_base_move_and_recal_design.md` updated this session with §3.2 + Q12; spec-sheet collection prerequisite).
6. **issue#21** — NEON `evaluate_scan` SIMD.
7. **issue#23** — LF prefetch / gather-batch.
8. **issue#7** — boom-arm angle masking.
9. **issue#17** — GPIO UART (perma-deferred).
10. **PR #96 / Phase-0 cleanup** — SUPERSEDED by PR #110 promotion (PHASE0 ext takes the pattern from "TEMPORARY env-gated" to "first-class default-on diag" via pose+sigma fields).
11. **issue#19.2 / 19.3 / 19.4** (deferred follow-ups).
12. **SPA Config tab search** (low-priority UX).

**Thirtieth-session metadata**:

- Wall-clock: ~16 hours (2026-05-07 17:00 KST → 2026-05-08 12:00 KST, sleep + 8 h overnight HIL included).
- 1 PR merged into main + 1 PR queued for merge + 1 docs PR (this chronicler output) to follow.
- 1 new memory entry (`project_round_a_range_min_m_sweep_2026-05-08.md` — Round A findings + Bug B mechanism + 4.5 m decision rationale + future-studio caveat).
- 1 existing memory entry annotated (`project_yaw_tripwire_design_flaw.md` → RESOLVED 2026-05-07 — issue#36).
- issue#37 + issue#37.1-.7 reservations established (pool sep / trip-recover / c1 deadline raise / EDT-side gate / windowed K-gate / snapshot extension / stress harness — all conditional on future HIL escalation triggers).
- issue#38 closed via PR #110 (cap bumps).
- issue#29 spec significantly expanded (§3.2 forward kinematics override + Q1-Q12).
- Bug B reclassified RESOLVED via issue#38 hard cutoff (was "analysis-first, operator-driven HIL needed" in 29th-session TL;DR).
- Next free issue integer: **`issue#39`** (was 37 before this session).

---

### 2026-05-06 → 2026-05-07 (evening + late-night — 19:00 KST → 08:30 KST, twenty-ninth-session — issue#19 EDT 2D 3-way parallelization ships through full pipeline → operator HIL on news-pi01 → measured 1.43× LF lift +19% Hz vs plan target 3× / +56% missed; algorithmic ceiling identified as memory-bandwidth-bound; 2 doc typos surfaced + folded; 2 new memory entries from operator brief)

Opened directly off twenty-eighth-session close (main = `9d95dda` after PR #102 AMCL doc). Operator brief at session-open: "이제 다음 작업 시작하자요 / 최신 상태 확인부터 git 고고고." Session ran issue#19 (TL;DR #1 in 28th-session NEXT_SESSION) through the full agent pipeline end-to-end — code-planner → code-reviewer Mode-A round 1 → code-writer (single wave with API 529 mid-burst, resumed via second invocation) → code-reviewer Mode-B round 1 → operator HIL on news-pi01 → HIL fold-in commit → PR #104 squash-merge. Pre-issue#19 quick docs cleanup PR #103 closed an early-session typo trail (1100 → 853 line count footer across 3 SSOT files). Total: 2 PRs merged, 1 docs PR (chronicler output) to be opened, ~13 hours wall-clock with sleep included.

**Notable structural revelation — EDT is memory-bandwidth-bound; 1.43× is the algorithmic ceiling on Pi 5, not a measurement artifact**. Plan §3.7 projected 3× LF rebuild lift (40 → ~14 ms) by symmetry with issue#11's `evaluate_scan` parallelisation. Operator HIL on news-pi01 (5-min Phase-0 capture, 3588 PHASE0 samples, isolcpus=3 boot, pool active 3 workers on CPUs 0/1/2): LF rebuild p50 **40.03 → 28.09 ms (1.43× lift)**, TOTAL p50 86.80 → 72.64 ms, cold-path Hz **11.52 → 13.77 (+19%)**, 0 `[pool-degraded]` events, bit-equality preserved. The 1.43× figure exactly matched the dev-box ctest concurrent measurement (1.4–1.5×) — production isolcpus=3 reproduced the same ceiling. Felzenszwalb 1D EDT does column-major reads through `intermediate[y*W+x]` with large strides → cache miss heavy → 3 workers in parallel saturate DDR4 memory bus on Pi 5 → sub-linear scaling. issue#11's evaluate_scan was compute-bound (per-particle math on read-only LF) and scaled cleanly to 2.10× on the same pool — the workloads are **structurally different**. Forcing more lift requires NEON SIMD vectorisation, prefetch directives, or transpose-tile cache locality — distinct algorithmic work, not a re-tune of the parallelisation. **Operator decision (2026-05-07 KST)**: ship at 13.77 Hz; reserve `issue#19.5` for the cache-locality / SIMD path.

**Process violation + lesson — 2 documentation typos propagated through plan + Mode-A round 1 + override.conf + post-deploy checklist**. Operator's first 5-min Phase-0 capture attempt produced **0 `[phase0]` lines** because (a) the env var was named `GODO_PHASE0_TRIM_ON` in the plan, but `cold_writer.cpp:38` actually reads `GODO_PHASE0` (strict `"1"` value match), so the override.conf set the wrong variable and the instrumentation was OFF; AND (b) the post-deploy `grep '\[phase0\]'` pattern (lowercase + brackets) didn't match the actual emit format `PHASE0 path=live_pipelined scan=...` (uppercase, no brackets). Both typos lived in plan + Mode-A round 1 + Mode-B (which inspected docs but not against source literals). Lesson: **Mode-A audit hooks A2 (env-var literal) and A4 (emit literal) must grep the source code for the exact string before signing off plan claims**. Locked in `feedback_cross_section_consistency_after_round_2_adds.md` family. Documentation amended in plan, W19 archive, PR body. Same family of failure as issue#11's §3.7/§4 cross-section consistency miss — that lesson was still being learned, this iteration adds the literal-vs-claim grep rule.

**Yaw tripwire reclassified as design flaw (operator brief 2026-05-07 KST, mid-HIL)**. The 5-min capture journalctl was dominated by 2994 `cold_writer: yaw tripwire fired` events (every Live tick whenever `pose.yaw - origin.yaw > 5°`). Designer's intent: warn the operator if the BASE rotates so calibration can be re-run. Reality: LiDAR sits on the crane's pan-axis center (CLAUDE.md §2/§9 physical invariant) → LiDAR yaw follows pan rotation 1:1 → tripwire fires every tick where the camera is panned more than ~2° from OneShot time, which is essentially all the time during a normal shoot. The "BASE rotates" scenario the tripwire was designed for is ruled out by physical invariants the project never violates. **Two new memory entries** captured this:
- `.claude/memory/project_yaw_tripwire_design_flaw.md` — full rationale, locks `issue#36` for elimination/threshold raise.
- `.claude/memory/project_oneshot_vs_mapedit_origin_yaw.md` — adjacent operator clarification: OneShot calibrates `(x, y)` only; `origin.yaw` is exclusively a MAP EDIT operation (issue#28/#30 lineage). Standard workflow: OneShot first → Live Mode every session. Rules out the misconception (which had drifted into early HIL doc) that "OneShot Calibrate to silence the yaw tripwire" — OneShot does not change `origin.yaw`, so the tripwire is unaffected.

**2 PRs landed this session**:

| PR | Issue | Title | State |
|---|---|---|---|
| #103 (`2579b4c`) | docs cleanup | docs: correct AMCL doc line count footer (1100 → 853) across SSOT trail | merged |
| #104 (`7a91806`) | issue#19 | feat(issue#19): EDT 2D Felzenszwalb 3-way parallelization | merged (squash 8 commits) |

PR #104 commit chain: P4-2-19-1 (Tier-1 constants + EdtScratch) → -2 (`parallel_for_with_scratch<S>` API + `[edt-scratch-asserted]` grep) → -3 (build_likelihood_field pool overload + cache-aligned column partition) → -4 (cold_writer plumbing + `Amcl::pool()` accessor) → -5 (`bench_lf_rebuild` + core-aware speedup floor) → -6 (SYSTEM_DESIGN §6.6.1 + CODEBASE invariant `(s.1)` + memory cascade + bench floor 1.6× → 1.1×) → Mode-B fold (m1/m2/m3/n1 docs cascade fix) → HIL fold (measurements + 2 typo fixes + memory entries). 22 files / +1758 / -60. P4-2-19-7 was folded into -6 per task instruction.

**Pipeline execution notes**:
- **Mode-A round 1 verdict**: APPROVE WITH MINOR REVISIONS (5m + 8n folded into plan; operator-locked m2 = bench-driven anchor fallback rule, anchor=1000 ship + downgrade to 750 if 2000×2000 worker p99 > 80 ms).
- **Writer wave hit API 529 mid-P4-2-19-5**; resumed via second writer invocation. P4-2-19-1..-4 had committed cleanly before the interruption (verified via build + 54/54 tests green). Branch state preserved by the commit-per-task cadence; no rework needed.
- **Mode-B round 1 verdict**: APPROVE WITH MINOR REVISIONS (3m docs-only + 1n folded inline at commit `2f3e7cb`). The 3m findings were stale-narrative drift across 4 docs locations (CODEBASE.md `(s.1)` + SYSTEM_DESIGN.md §6.6.1 + W19 archive + memory file) that described an early-draft `30 ms / 21,845 cells / scale~15` numeric set that did NOT match the actual constants `50 ms / 1000-dim / scale=1 at 1000²`. Caught pre-squash.
- **HIL fold-in commit** `4dafd5b` absorbed the 2 doc typos + measurements + memory cross-links. Squash commit body amended via `gh pr edit 104 --body` to populate the `??` placeholders before final merge.

**Open queue for next session** (priority order, operator-locked):

1. **issue#19.5** — EDT cache locality / SIMD optimisation (TOP PRIORITY now that issue#19 main is shipped). Candidate paths: NEON vectorisation of `edt_1d` inner loop, `__builtin_prefetch` directives, transpose-tile cache locality, row-major rewrite of the column pass. Reuses `parallel_for_with_scratch<S>` infrastructure. Bench-driven exploration first, then commit to a path.
2. **issue#36** — `cold_writer.cpp` yaw tripwire elimination or threshold raise to ≥ 90° (operator preference: eliminate). Trivial PR (~10 LOC removal) but Mode-A should validate that no other code reads the tripwire's stderr output.
3. **issue#13** — distance-weighted AMCL likelihood (`r_cutoff` near-LiDAR down-weight). Orthogonal to issue#11/#19; modifies `evaluate_scan` body, not the caller.
4. **issue#26** — cross-device latency measurement tool (paused at Mode-A round 1 REJECT; independent of issue#11/#19 now).
5. **issue#29** — SHOTOKU base-move + two-point cal-reset workflow (depends on issue#30).
6. **issue#21** — NEON/SIMD vectorisation of `evaluate_scan` per-beam loop (Cortex-A76 NEON; ~2-3× per-particle speedup).
7. **issue#22** — KLD-sampling adaptive N.
8. **issue#23** — LF prefetch / gather-batch.
9. **issue#7** — boom-arm angle masking (contingent on issue#4 diagnostic).
10. **issue#17** — GPIO UART (perma-deferred).
11. **Bug B** — Live mode standstill jitter (analysis-first).

**Twenty-ninth-session metadata**:

- Wall-clock: ~13 hours (2026-05-06 19:00 KST → 2026-05-07 08:30 KST, sleep included).
- 2 PRs merged + 1 docs PR (this chronicler output) to follow.
- 2 new memory entries (yaw tripwire design flaw + OneShot-vs-MAP-EDIT origin yaw).
- 6 reserved follow-up issues (`issue#19.1..5` decimal sub-issues + `issue#36` integer).
- Next free issue integer: **`issue#37`** (was 36 before this session).

---

### 2026-05-06 (afternoon — 13:30 KST → 18:00 KST, twenty-eighth-session — issue#11 Mode-A round 2 ships, full pipeline through Writer → Mode-B → PR #99 merge → post-deploy critical defect caught + range-proportional deadline fix → fresh 5-min Phase-0 capture validates 7.34 → 11.52 Hz lift +57%)

Opened directly off twenty-seventh-session close (main = `129ad3f` after PR #98 Parent territory updates). Operator brief: "Phase-0 데이터 측정 완료, 정밀 분석하고 issue#11 Mode-A round 2 들어가자." The session ran the full agent pipeline end-to-end on issue#11 — code-planner → code-reviewer Mode-A round 2 → code-writer → code-reviewer Mode-B → PR #99 squash-merge — and then absorbed a post-deploy critical defect via a range-proportional deadline fix. PHASE0 5-min capture on the post-fix binary empirically validated **+57% cold-path Hz lift** (7.34 → 11.52 Hz, 10 Hz LiDAR cleared with margin).

**Notable structural revelation #1 — issue#11 main implementation lifts the cold path past the 10 Hz LiDAR rate.** Plan §3.7 first-tick anatomy projected ~190 ms for parallel N=5000 first-tick; §4 set a 50 ms hard timeout. All three review passes (Mode-A round 1, round 2, Mode-B) verified each section against its own claims, but **none of them cross-multiplied the two**. Operator HIL caught it on first deploy: 1m 49s after `ParallelEvalPool ready (workers=3)`, the production tracker emitted `[pool-degraded] parallel_for join exceeded 50 ms hard deadline (workers=3, range=[0,5000))` and transitioned to permanent inline-sequential mode. Pool was alive but every dispatch ran sequentially for the rest of the tracker's lifetime. **Fix**: range-proportional deadline `kJoinTimeoutBaseNs × max(1, range / kJoinTimeoutAnchorN)` (base 50 ms anchored on N=500). Steady-state Live N=500 → 50 ms preserved (R5 worker-stall guard intact); first-tick N=5000 → 500 ms (~2.5× margin over §3.7 ~190 ms parallel projection). The fix preserves the original safety semantic AND honors §3.7's mathematical projection; the §3.7/§4 self-inconsistency is now resolved structurally, not papered over.

**Notable structural revelation #2 — Phase-0 measurement vs Phase-0 fresh-capture provide a direct sequential vs parallel comparison.** The same 5-min, ~3000-scan capture procedure that grounded Mode-A round 2 in 27th-session was re-run on the post-fix binary in 28th-session. Per-component p50 numbers (sequential baseline → post-fix Option C):
- evaluate_scan: 94.85 → **45.11 ms** (2.10× speedup ✓ — confirms §3 fork-join projection)
- LF rebuild: 39.58 → 40.03 ms (unchanged — issue#19 territory)
- jitter / normalize / resample: <1 ms each (unchanged)
- TOTAL: 136.15 → **86.80 ms** (1.57× speedup)
- Cold-path Hz: 7.34 → **11.52 Hz** (+57%, clears 10 Hz LiDAR with margin)
- `[pool-degraded]` events during capture: **0** (range-proportional fix verified)
- CPU 0/1/2 mean busy: 65.3 / 67.5 / 66.5% (workers balanced; no single-core saturation)
- CPU 3 mean busy: 0.1% (RT contract preserved end-to-end; jitter unchanged from baseline)

Issue#19 (EDT 2D 3-way parallelization) is the next target since LF rebuild is now the dominant slice (46% post-fix vs 29% pre-fix in absolute share); the Option C+#19 projection ~21 Hz remains valid.

**Process violation + lesson — cross-section quantitative consistency must be re-verified after late-stage section adds.** Mode-A round 1 reviewer flagged "first-tick anatomy" as a missing analysis (M2). Mode-A round 2 reviewer closed M2 by adding §3.7 ("OneShot N=5000 first-tick: ~580 ms sequential → ~190 ms parallel"). Mode-A round 2 reviewer ALSO verified §4's 50 ms deadline against its own logic but did not cross-check against §3.7's freshly-added projection. Mode-B reviewer pinned the 50 ms code at `parallel_eval_pool.cpp:362-364` against the §4 spec without checking §3.7 either. The defect existed only in the **interaction** between §3.7 and §4 — neither section was wrong in isolation. Operator HIL caught it within 2 minutes of deploy. **Lesson candidate** (Parent territory at session-close): write a feedback memory `feedback_cross_section_consistency_after_round_2_adds.md` — when a Mode-A round close adds a new subsection (§3.7 here), the next reviewer pass must re-cross-check that subsection's quantitative claims against the rest of the plan, not just verify the subsection's internal consistency.

**Process pattern — range-proportional workload-deadline as reusable recipe.** Whenever a fork-join hot path is dispatched with workload sizes that vary by 10× across modes (steady N=500 vs first-tick N=5000), a flat hard timeout is mathematically incompatible with both modes. The pattern `deadline ∝ max(1, workload / steady_anchor)` preserves the worker-stall guard for the steady mode AND accommodates the heavy mode within the same architectural surface. Same recipe applies to (potentially): EDT row/col passes (W × H grid sweep, scales with map size), map activate phased reload, Phase 5 UE characterization runs. Lesson candidate (Parent territory): `project_range_proportional_deadline_pattern.md`.

**1 PR landed this session**:

| PR | Issue | Title | State |
|---|---|---|---|
| #99 | issue#11 | feat(issue#11): Live mode pipelined-parallel AMCL particle eval pool | merged `64a2abb` (squash, 9 commits → 1 line on main) |

**PR #99 commit composition (squashed into `64a2abb`)**:

| Commit | Task | Notes |
|---|---|---|
| `aba046e` | P4-2-11-1 | New `src/parallel/` static lib `godo_parallel`. ParallelEvalPool ctor signature `explicit ParallelEvalPool(std::vector<int> cpus_to_pin)`; pimpl-clean header (no `<mutex>`/`<condition_variable>` exposed); CPU 3 hard-vetoed at ctor; `parallel_for(begin, end, fn)` + (initially flat) 50 ms timeout + R5 lifetime hazard handled (fn moved into Impl-owned storage so caller stack-allocated lambdas can't dangle on timeout). |
| `08c59ed` | P4-2-11-2 | `Amcl(cfg, lf, ParallelEvalPool* pool = nullptr)` ctor + `step()` body branches on pool with sequential fallback if `parallel_for` returns false. `weighted_mean()` body unchanged sequential summation in i-order (§3.6 bit-equality preserved). Phase-0 hooks at amcl.cpp:194-260 preserved verbatim. |
| `f00b953` | P4-2-11-3 + -4 | `run_cold_writer(..., ParallelEvalPool* pool = nullptr)` + `main.cpp:519-543` spawns pool BEFORE cold writer thread (R11 ordering). New TOML row `amcl.parallel_eval_workers` Int [1, 3] default 3 Recalibrate-class; full plumbing (env / CLI / TOML reads, validation, apply.cpp); schema row count 67 → 68. |
| `6142de0` | P4-2-11-5 | New dedicated `Seqlock<ParallelEvalSnapshot>` (NOT extending `JitterSnapshot` per Mode-A round 2 M9 decision); fields = `dispatch_count`, `fallback_count`, `p99_us`, `max_us`, `degraded`, padding. New UDS endpoint `get_parallel_eval` + `format_ok_parallel_eval` JSON writer. Diag pump samples per-dispatch. |
| `c764131` | P4-2-11-6 | Tests: `test_parallel_eval_pool.cpp` (9 cases / 277 LOC), `test_amcl_parallel_eval.cpp` (5 cases / 271 LOC, bit-equal `memcmp` IEEE 754 pin), `bench_amcl_converge.cpp` (2 cases / 228 LOC, 1.5× / 1.2× CI floors). |
| `cb97618` | P4-2-11-7 | Docs: CODEBASE.md invariant `(s)` body + weekly archive `production/RPi5/CODEBASE/2026-W19.md` dated entry + `SYSTEM_DESIGN.md` §6.6 added (1071-1156); memory file `project_pipelined_compute_pattern.md` marks issue#11 as DONE. |
| `e07b7c0` | Parent diagnostics fix | Removed unused `kWorkerStackBytes` constant (Writer's deviation #2 deferred 256 KB stack cap; honest comment on `workers_` field) + 2 unused includes. |
| `5bb7c03` | Mode-B M-1 + M-2 docs | Reconciled bench band drift (CODEBASE.md `(s)` + W19 archive: "≥ 2× / ≥ 1.5×" → "≥ 1.5× / ≥ 1.2× CI floor; standalone observed 2.86× / 1.76×; production isolcpus=3 expected ~3×"). Replaced `amcl.cpp:297-314` line cite with function-anchored `Amcl::weighted_mean() body in amcl.cpp` cite. |
| `bfbf671` | Range-proportional fix | post-deploy HIL critical defect; see structural revelation #1 above. |

**Cross-cutting lessons for the rule book**:

- **Multi-pass review can collude on a self-inconsistency** if each pass verifies sections individually but no pass cross-multiplies them. Write the lesson as a feedback memory entry with a concrete cross-check list (e.g., "if §X adds a quantitative claim, audit §Y and §Z claims that constrain it").
- **Range-proportional pattern** as reusable for any hot path whose workload varies by 10× across modes.
- **Squash-merge with delete-branch** keeps `main` history one-line-per-PR clean; the per-task commit boundary lives only on the feature branch and is captured here in the change-log table for forensic value.

**Open queue for next session** (priority order, operator-locked):

1. **issue#19** — EDT 2D Felzenszwalb 3-way parallelization. Reuses issue#11's `ParallelEvalPool` primitive with the `parallel_for_with_scratch<S>` API extension that Mode-A round 1 C6 surfaced (per-worker `(v, z)` scratch buffers). Projected lift: 11.52 → ~21 Hz (LF rebuild 40 → ~14 ms). ★ TOP PRIORITY now that issue#11 main is shipped.
2. **AMCL algorithm analysis docs PR** — `/doc/amcl_algorithm_analysis.md` already written (853 lines, 11 Parts × 32 sections, file:line cited from main `129ad3f` + branch HEAD; intentionally kept untracked this session per operator decision). Open as a separate docs PR in next session.
3. **issue#13** — distance-weighted AMCL likelihood (`r_cutoff` near-LiDAR down-weight). Orthogonal to issue#11 (modifies `evaluate_scan` body, not the caller); composes with Option C without re-design. Standalone single-knob algorithmic experiment.
4. **issue#35 (still candidate)** — UDS hang after RPLIDAR I/O fallback. Sample size 2 from 27th-session's mid-session incident; not reproduced this session despite build+ctest CPU saturation. Track for next reproduction; investigate `cold_writer.cpp::run_cold_writer` exception path's interaction with `uds_server.cpp` accept loop.
5. **issue#26** — cross-device latency measurement tool. Mode-A round 1 REJECT, paused; independent of issue#11 now.
6. **issue#29** — SHOTOKU base-move + two-point cal-reset workflow. issue#30 dependency resolved.
7. **issue#21** — NEON/SIMD vectorization of `evaluate_scan`. Compatible with issue#11; orthogonal speedup ~2-3× per particle on top of fork-join.
8. **issue#22** — KLD-sampling adaptive N. Big payoff potentially exceeds issue#11+#19 combined; intricate machinery though.
9. **PR #96 / Phase-0 cleanup decision point** — TEMPORARY contract honored if reverted; OR promote to permanent diag via documented mid-life path (Phase0BreakdownSnapshot Seqlock + UDS getter + `[phase0-publisher-grep]` build-grep + invariant `(s)` promotion). Defer until next operator HIL run uses it again.
10. **issue#7** — boom-arm angle masking. Optional; contingent on issue#4 diagnostic.

---



Opened directly off twenty-sixth-session close (PR #94 merged at session start, main = `61fc445`). Operator brief: continue the Live mode CPU thrashing investigation surfaced during PR #93 HIL. Long monitoring window (~12.5 hours) confirmed pattern is **normal-as-designed** — the "all 4 cores 100%" perception is a rendering artifact of single-thread CFS migration, not actual 4-way saturation. Two PRs landed in the session arc, plus extensive HIL captures that resolved a long-standing numerical foundation gap for issue#11.

**Notable structural revelation #1 — Live mode CPU thrashing is normal-as-designed**. Twenty-sixth-session captured the initial macroscopic shape (TID 3438307 cold-path AMCL kernel sustaining 40-60% CPU; CFS migrating across cores 0/1/2). This session built a long-running monitor (`/tmp/cpu_monitor.py` — 5s sample interval, alert at 4-core simultaneous ≥80%) running ~12 hours under Live mode and a 1s-fastcap ~2 minute window. Findings: (a) **`/sys/devices/system/cpu/isolated = 3`** confirms isolcpus kernel parameter IS applied (twenty-sixth-session hypothesis "may not be applied" rejected); (b) **`/proc/cmdline` includes `isolcpus=3 nohz_full=3 rcu_nocbs=3`** — full hardening present; (c) **CPU 3 idle 99.6%, RT thread state=S, jitter p99=18.5 µs** — hot path isolation rock-solid; (d) **single cold thread sustains 97-98% on whichever cpu 0/1/2 it's parked on**, with CFS migrating it every 3-10 s (median 3 s on 1 s sampling). In any 60 s observation window the cold thread visits all 3 user cores 100% of the time — htop's per-core bars at sub-second cadence will visually fill all 3 cpu 0/1/2 bars within any 1-minute viewing, plus brief cpu3 lighting from kernel work. Operator's "all 4 cores 100%" was rendering artifact, not actual 4-way saturation. Memory file `.claude/memory/project_live_mode_cpu_thrashing_2026-05-05.md` updated with "Resume + reclassification" section locking the diagnosis. **Net**: pattern is the prepared answer for issue#11 (Live pipelined-parallel multi-thread), not a separate bug.

**Notable structural revelation #2 — issue#11 numerical foundation closed via Phase-0 trim instrumentation**. The plan `.claude/tmp/plan_issue_11_live_pipelined_parallel.md` was Mode-A REJECTed at round 1 (twentieth-session) for unverified per-component compute times. This session ran a Mode-A round 1 review of the Phase-0 instrumentation plan; reviewer flagged scope inflation (planner's "~30 LOC, removable" upstream brief expanded to ~500 LOC, 11 test cases). Operator decision-locked **lane (i) trim** over lane (ii) full ship-vs-wire (rationale: this commit's purpose is to enable Mode-A round 2 — needs 5-6 numbers per scan, not a polished SPA surface). Mechanical Mode-A findings (C1 size math / C2 on_leave_live signature / C3 run_anneal_kernel plumbing / M2 R4 risk framing / M4 bool-vs-int) all moot under the trim contract (no Seqlock, no TOML key, no Config field — env-var + thread-local + stderr emit). Trim path implemented + Mode-B APPROVE + merged as PR #96. HIL on news-pi01 (3 capture windows totaling 2,716 scans across 2 PIDs) locked in: **p50 TOTAL = 136.15 ms / 7.34 Hz; evaluate_scan = 94.85 ms (69.7%); LF rebuild = 39.58 ms (29.1%); jitter+normalize+resample combined < 1%; residual 0.5%**. Cross-capture variance < 0.5% on each component — empirically locked. Implication for Option C alone: 39.6 + 31.6 + 1.0 + 0.7 ≈ **73 ms → 13.7 Hz** (clears 10 Hz LiDAR with 37% margin). Option C + issue#19 (EDT row/col 3-way): **47 ms → 21.4 Hz** (114% margin). issue#19 elevated from "follow-up" to "important secondary" since LF rebuild is 29% not the originally back-of-envelope 30% but in absolute terms a meaningful chunk.

**Notable structural revelation #3 — `doc/issue11_design_analysis.md` §2.6 Option B vs C FAQ added**. Operator flagged misunderstanding mid-session: "옵션 (C)의 입자별 분할을 하여 각각의 위치를 추정한다는 것인지? 지금 보니 각 입자는 순서대로 1/3씩 쪼개는 것 같은데, 그러면 어느 한 곳의 라이다 출력만 보게되니 특징점을 더 잡기가 어려워서 AMCL 위치 추정이 어렵지 않을까?" — operator briefly read Option C's "particles split 1/3 each" as "scan input split 1/3 each" (which would degrade AMCL accuracy). Clarification documented as new §2.6 in `doc/issue11_design_analysis.md`: time-axis split (Option B tier-pipeline) vs data-axis split (Option C fork-join over particles), with explicit "the scan is read-only and shared across all workers" pin + grading-rubric analogy + Unicode box-drawing diagram + issue#13 (distance-weighted likelihood) compatibility section. **Pattern is reusable** for future "what's split" misreadings.

**Process violation + lesson — `systemctl edit` empty-content gotcha**. Operator attempted to disable `Environment="GODO_PHASE0=1"` after Phase-0 measurements landed via `sudo systemctl edit godo-tracker.service`. systemd's behavior: "after editing, new contents are empty, not writing file" — refuses to write empty override.conf, leaves existing override in place. Operator hit Ctrl+X without saving non-empty content; edit silently no-op'd. Net effect: PHASE0 stayed enabled across the operator-thought-disabled restart; current PID 4123500 still emitted PHASE0 lines. Diagnosis took ~30 sec (`systemctl show -p Environment` → `Environment=GODO_PHASE0=1`; `cat override.conf` → file unchanged at `mtime 09:47`). Recovery path: direct file delete `sudo rm override.conf` + `rmdir` + `daemon-reload` + `restart` (option B). **Lesson candidate** (Parent territory at session-close): add a feedback memory entry — `feedback_systemctl_edit_empty_content_no_save.md` — covering "if you intend to remove a directive, replace with `# disabled` comment, not empty file; OR delete the override file directly". Mode-B reviewers should call out any docs that say "delete the line" without warning about systemd's empty-file refusal.

**Process discovery — UDS hang after RPLIDAR I/O fallback to Idle (candidate issue#X)**. Mid-session the production tracker (PID 1708747, ~12h uptime) became UDS-unreachable: `webctl /api/health` returned `tracker_unreachable`; SPA showed "godo-tracker가 응답하지 않습니다"; direct UDS connect via Python socket → `Connection refused`. `ss -lx` showed `LISTEN 0 0 /run/godo/ctl.sock.1708747.tmp` — backlog=0 means kernel rejects accept(). Process itself was alive (all threads state=S, no zombies). Last `journalctl` line at 09:22:05: `cold_writer: scan_frames(1) threw in Live: rplidar: grabScanDataHq failed 5 times in a row — returning to Idle.` Triggered by Parent's background `bash scripts/build.sh` saturating cpu0/1/2 100% during PR #96 build, starving the USB scan-reader thread → 5 consecutive `grabScanDataHq` timeouts → cold_writer caught the exception and gracefully transitioned to Idle. **But** the UDS server thread's listen-backlog state went to 0 simultaneously — separate invariant. Tracker remained alive but unable to accept any UDS connection until restart. **Candidate**: track as a new issue#X (Medium priority — rare in normal ops, hard to reproduce without artificial cpu saturation; fix would investigate `cold_writer.cpp::run_cold_writer` exception path's interaction with UDS server thread). Recovery for the immediate session was the operator-driven `systemctl restart godo-tracker` which preceded the Phase-0 HIL.

**2 PRs landed this session**:

| PR | Issue | Title | State |
|---|---|---|---|
| #95 | issue#28.2 | test(issue#28.2): pin SSE producer-side end-to-end for /api/map/edit/coord | merged `a0a3113` (squash, single commit) |
| #96 | issue#11 P4-2-11-0 | feat(issue#11): P4-2-11-0 trim Phase-0 cold-path component instrumentation | merged `53453f5` (squash, single commit) |

**PR #95 (issue#28.2)** — closes the producer-side SSE gap PR #93 Mode-B M1 carryover flagged. Adds 2 integration test cases in `godo-webctl/tests/test_map_edit_sse_e2e.py`: (T1) happy path coord apply → frames carry consistent `request_id` matching HTTP response, first frame `starting` (progress=0.0), last `done` (progress=1.0), monotonic progress, `done` carries `derived_name`; (T2) reject path with empty maps_dir → `starting` then `rejected` with non-empty `reason=active_map_missing`, request_id consistent. Drive-by cleanups: `tests/test_app_hardware_tracker.py` failing on production host because `/api/calibrate` is admin-only — added login + `Authorization: Bearer` header following test_map_edit_concurrency.py pattern; made `hardware_tracker` marker actually skipped by default via `addopts = -ra -m 'not hardware_tracker'` in `pyproject.toml` (matches the marker comment's stated intent — original config silently leaked into default runs on hosts where `/run/godo/ctl.sock` exists). 1058 webctl pytest pass + 1 deselected. HIL ran the hardware_tracker test on news-pi01 successfully — `1 passed in 2.31s`, calibrate auth fix verified end-to-end.

**PR #96 (issue#11 P4-2-11-0)** — TEMPORARY measurement tool. New `godo::rt::Phase0InnerBreakdown` POD struct (32 B, 4× int64_t) in `core/rt_types.hpp`. New `Amcl::step` 5-arg + 3-arg overloads accepting `Phase0InnerBreakdown*` out-param; existing 4-arg + 2-arg overloads delegate with nullptr (zero-overhead path preserved). `cold_writer.cpp` adds anonymous-namespace `kPhase0On` env latch (read once at static init from `getenv("GODO_PHASE0")`), thread-local accumulators, helpers `phase0_reset_and_stamp_start` + `phase0_emit`. Wired into all 3 wrappers (`run_one_iteration`, `run_live_iteration`, `run_live_iteration_pipelined`) with `path=` label distinguishing OneShot/Live/Live-pipelined. Output line: `PHASE0 path=live_pipelined scan=N iters=K lf_rebuild_ns=... jitter_ns=... eval_ns=... norm_ns=... resamp_ns=... total_ns=...`. New test `tests/test_phase0_env.cpp` (3 cases / 16 assertions) — sentinel-pre-fill (deterministic, NOT timing-based), nullptr-no-write, sizeof+trivially-copyable layout pin. Build greps clean: `[m1-no-mutex]` cold_writer.cpp untouched (monotonic_ns + thread-local + Seqlock-free path). 49/49 ctest hardware-free + 1/1 python-required pass. Mode-B reviewer APPROVE (zero blockers, 5 minor non-blocking observations). Operator HIL captured 3 windows totaling 2,716 scans → results in `.claude/tmp/phase0_results_5min_20260506_095949.md` (main dataset, 2166 scans / 5 min).

**Cross-cutting lessons for the rule book**:

- **Long-running monitor as session-context tool**: 5s-interval CSV + 30s human-readable heartbeat + alert mechanism for 4-core simultaneous ≥80% × 2 consec — pattern preserved as `/tmp/cpu_monitor.py` + Monitor-task-based stdout streaming. Reusable for any future "is X behavior reproducible across hours" question. CSV path + snapshot directory survive across the session boundary as a re-occurrence baseline.
- **TEMPORARY commit shape**: PR #96 is the prototype. env-var-gated + thread-local + stderr emit + single test. Revertable by single `git revert <sha>`. Promotable to permanent ship-vs-wire if next-session usage validates retention.
- **Trim vs polished tradeoff**: trim path delivered ~70 LOC of production source + 1 test in a single Writer pass; full ship-vs-wire would have been 6-8 hour cycle (Mode-A round 2 + Writer + Mode-B + HIL). For "data feeds Mode-A round 2" use case, trim wins decisively.

**Open queue for next session** (priority order, operator-locked):

1. **issue#11 Mode-A round 2** — fold Phase-0 numbers (p50 TOTAL=136 ms / eval=70% / LF=29%) into `.claude/tmp/plan_issue_11_live_pipelined_parallel.md` Round 2 fold. Replace back-of-envelope estimates with measured numbers; re-derive K-budget, σ-schedule trade-offs, Option B/C/D scoring under empirical foundation.
2. **issue#11 main implementation** (post-round 2 approve) — Option C fork-join particle eval pool (~280 LOC + tests). Mode-A round 1 mechanical findings (C4 schema row count: today is 67, new row → 68; C5 invariant letter `(s)`; C6 EDT scratch buffer; C7 M1 spirit articulation) absorb into the new plan.
3. **issue#19** — EDT 2D Felzenszwalb row/col 3-way parallelization. Elevated from "follow-up" to "important secondary" — brings projection 13.7 Hz → 21.4 Hz. Same `ParallelEvalPool` primitive (no new module; just the `parallel_for_with_scratch<S>` API extension that Mode-A round 1 C6 surfaced).
4. **issue#X (new) — UDS hang after RPLIDAR I/O fallback to Idle**. Trigger: `cold_writer.cpp::run_cold_writer` exception path interacts badly with UDS server thread's accept loop. Symptom: `ss -lx` `LISTEN 0 0` (backlog=0). Repro: artificially saturate cpu 0/1/2 (e.g., concurrent build) until RPLIDAR USB scan-reader hits `grabScanDataHq` 5× timeout; cold_writer falls back to Idle but UDS remains broken. Recovery: `systemctl restart godo-tracker`. Medium priority — rare in normal ops.
5. **issue#13** — distance-weighted AMCL likelihood (`r_cutoff` near-LiDAR down-weight). Orthogonal to issue#11 (modifies `evaluate_scan` body, not the caller); composes with Option C without re-design. Standalone single-knob algorithmic experiment.
6. **issue#26** — cross-device latency measurement tool (Mode-A round 1 REJECT, paused). Independent of issue#11 now that Phase-0 produced the in-tracker breakdown.
7. **issue#7** — boom-arm angle masking (optional, contingent on issue#4 diagnostic).
8. **issue#28.2** — DONE in PR #95. Removable from queue.
9. **PR #96 / Phase-0 cleanup decision point** — after Mode-A round 2 absorbs the numbers, decide: revert PR #96 (trim contract honored — TEMPORARY) OR promote to permanent diagnostic via the documented mid-life path (Phase0BreakdownSnapshot Seqlock + UDS getter + webctl endpoint + `[phase0-publisher-grep]` + invariant `(s)` promotion).

---



Twenty-sixth-session opened directly off the twenty-fifth-session close (PR #90 docs at `0113f98` ~03:39 UTC → main). Operator brief: TL;DR #1 was the deferred issue#28.1 (B-MAPEDIT-3 follow-ups). After Parent's verify-before-plan check confirmed CR1/CR2/CR3/MA2/MA10/MA6 had been silently absorbed during the issue#30 / 30.1 / 33 work flow, scope narrowed from "13 items" to "11 items still open + the 3 cleanup deletions operator emphasized". Operator then made a pivotal scope addition: **doc hierarchy reorganization** — five SSOT files (3 per-stack CODEBASE.md, root PROGRESS.md, doc/history.md) had grown to 16,586 combined lines across change logs and were becoming context-burdensome. The session pivoted to a two-phase plan: **Phase A** (doc hierarchy migration, no code) shipped first as PR-A; **Phase A.5** (CLAUDE.md polish + nav footer consistency) shipped as PR-A.5 right after; **Phase B** (issue#28.1 backlog) shipped as PR-B last.

Net result: **3 merged PRs in 3.5 hours** plus the day's largest single-session structural change.

**Notable structural revelation #1 — parallel-agent migration as a coordinated reorg pattern**. The doc hierarchy migration touched 5 master files (sizes 1420–4992 lines) with identical mechanical operations per file (split scaffold from change-log, write weekly archives, replace change-log with Index). Operator authorized "병렬로 여러 개의 에이전트를 돌려서 조사하고 정리하는 것이 일관성 있을 것 같다" — accepting per-agent context overhead for cross-file consistency. Five `code-writer` subagents launched in parallel, each given the same 7-step verification template + verbatim Index/header schema, each operating on a single file. All five reported back independent verification (line counts, sha256 byte-identical for PROGRESS.md's 1232-line session body, Korean encoding integrity for doc/history.md). Conservation per agent: zero entries lost across the 5-file migration. **Pattern is reusable** for future bulk-reorg work; plan + agent prompts preserved in `.claude/tmp/plan_issue_28.1_and_doc_hierarchy.md`.

**Notable structural revelation #2 — Live mode cold-path single-thread CPU thrashing**. During PR #93 HIL on news-pi01, operator-driven Live mode toggle triggered immediate CPU monitoring. Initial 8-second window (`top -H` per-thread × 5 samples): **TID 3438307** (cold-path AMCL kernel) sustained 40-60% CPU on a single core continuously, while RT hot path TID 3438312 (`cpus_allowed=3`, `priority -51`, SCHED_FIFO) stayed at 0.25% — hot path isolation working at this snapshot. CFS migrated TID 3438307 between cores 0/1/2 every few seconds — exact match for operator's "한 코어 100% → 다른 코어 100%" pattern observation. utime/stime ratio = 99.8% user-mode → particle filter loop, not syscall thrash. **Long-running follow-up (~1-2 hours later)**: operator observed ALL FOUR cores pegged at 100%, including CPU 3 — RT isolation appears to break down under sustained operation. Possible mechanisms: particle-cloud growth, cache thrashing, scan backlog buildup, or `isolcpus=` kernel parameter not actually applied. Diagnostic command snippet captured in memory for next session's first action. **This is the empirical motivator for issue#11 (Live pipelined-parallel multi-thread)** — Mode-A round 1 had REJECTed on numerical foundations (K≈16 not K=1, N=500 not N=10000); rerun with this CPU data + the issue#30 σ-schedule resolution should land cleaner.

**Process violation + lesson — TOML preflight regex bug**. The B12 hard-removal of `cfg.amcl_origin_yaw_deg` required the operator to strip the key from `/var/lib/godo/tracker.toml` before installing the new tracker binary (per `feedback_toml_branch_compat.md` strict-validator pattern). Parent's published preflight command was `sudo sed -i '/^amcl\.origin_yaw_deg/d' /var/lib/godo/tracker.toml` — written in dotted-key form. The actual TOML file uses **table syntax** (`[amcl]` section + `origin_yaw_deg = 0` inside it), so the regex matched zero lines. Operator ran preflight; install proceeded; new tracker on first boot hit `unknown TOML key 'amcl.origin_yaw_deg'`; systemd auto-restart loop hit count 70+. Root-cause diagnosis took ~10 min (curl `/api/health` → tracker unreachable; direct UDS test via Python socket → `ConnectionRefusedError`; `journalctl -u godo-tracker -n 100` → unknown-key reject log line; `cat /var/lib/godo/tracker.toml` → confirmed `origin_yaw_deg = 0` still present). Recovery: corrected `sudo sed -i '/^origin_yaw_deg = /d' /var/lib/godo/tracker.toml` (bare key, schema-unique to `[amcl]` section). Tracker green. **Lesson absorbed into `feedback_toml_branch_compat.md`** as a new section: "preflight commands must be tested against actual file shape, not the dotted-key mental model from the schema". Mode-B reviewer should add a "preflight regex shape-check" step to the deploy-readiness review block.

**3 PRs landed this session**:

| PR | Issue | Title | State |
|---|---|---|---|
| #91 | issue#34 | chore(issue#34): doc hierarchy weekly archive migration (Phase A) | merged `6870475` (squash, single commit) |
| #92 | issue#34.1 | chore(issue#34.1): CLAUDE.md polish + per-stack nav footers (Phase A.5) | merged `7cb76e8` (squash, single commit) |
| #93 | issue#28.1 | feat(issue#28.1): B-MAPEDIT-3 follow-up backlog batch (B1..B13) | merged `27669fc` (squash, single commit) |

PR #91 internals:
- 5 master files: godo-webctl 4293→1034 (-3259), godo-frontend 3920→611 (-3309), production/RPi5 4992→761 (-4231), PROGRESS 1420→196 (-1224), doc/history 1961→21 (-1940). Total 16,586 → 2,623 (-13,963).
- 14 weekly archive files (3 weeks × {godo-webctl, godo-frontend, production/RPi5} + 3 weeks for PROGRESS + 3 weeks for doc/history; godo-frontend has only W18+W19 since W17 had no entries) + 5 README.md orientation files.
- Operator-locked **Option (b)**: master keeps invariants/scaffold + Index ONLY — no inline most-recent dated entry. The weekly archive holds the dated body.
- CLAUDE.md §5 + §6 absorbed the new convention. `.claude/agents/{code-writer, code-reviewer, code-planner}.md` updated. `.claude/memory/feedback_codebase_md_freshness.md` "How to apply" rewritten for post-issue#34 structure.
- Conservation verification per agent: line counts exact match (sha256 byte-identical for PROGRESS body); zero entries lost.

PR #92 internals:
- CLAUDE.md 459 → 371 (-88): §3 Phases ASCII timeline → 1-paragraph current-phase line; §4 hardware connection → 1 line; §5 directory tree (56 lines) → 13-line "Doc hierarchy at a glance" bulleted nav (full tree lives in root CODEBASE.md); §9 open questions table → 6-line summary (Q4/Q5 still open).
- KEEP verbatim: §6 Golden Rules, §7 Agent pipeline, §8 Deploy + PR workflow, §10 References (operating-rule core).
- Per-stack nav footers added at the bottom of godo-webctl/CODEBASE.md, godo-frontend/CODEBASE.md, production/RPi5/CODEBASE.md — mirroring the PROGRESS.md `## Quick reference links` pattern operator approved at PR #91 close. Each footer points at: project guide, cross-stack scaffold, relevant design doc, sibling stacks (with one-line "how stacks connect" hint), project state, recent week archive, README.
- production/RPi5/CODEBASE.md:30 stale "Module map (current — as of 2026-04-26 Phase 4-2 D Wave B)" + "per-date entries below this section" claim fixed (post-archive, both inaccurate).

PR #93 internals (49 files, +1028 / -956, 4 deletions, 3 new test files, 12 new test cases):
- B1 `test_concurrent_apply_serialises[_or_rejects_busy]` (asyncio.Lock pin, 2 cases). B2/B3/B4 SSE invariants (broadcaster relay pin: monotonic floats / rejected-on-overflow / request_id stamping).
- B5 `OverlayToggleRow.test.ts` (4 cases: default state, per-toggle wiring, scanOverlay store mirror, three-toggle independence). B6 `overlayToggles.test.ts` (3 cases: v0 → v1 sessionStorage migration).
- B7 docstring `→ +170°` → `→ −170°`. B8 `LANCZOS_FILTER_NAME` workaround removed (constant + import + marker line). B9 `KRAD_TO_DEG` promoted to `production/RPi5/src/core/constants.hpp::godo::constants` (Writer's deviation from plan's `godo::core` namespace — `godo::constants` matches existing convention); `occupancy_grid.cpp:379` magic literal `(180.0 / 3.14159265358979323846)` replaced.
- B11 standalone `OriginAxisOverlay.svelte` + `GridOverlay.svelte` deleted (PR #81's HIL fix already moved overlay drawing into PoseCanvas's drawPoseLayer; standalone components were dead code). Comments in `lib/constants.ts` + `stores/overlayToggles.ts` rewritten; FRONT_DESIGN.md:309 past-tense.
- B12 `cfg.amcl_origin_yaw_deg` HARD-REMOVED across 9 source-tree sites + 8 test sites. Schema array size 68 → 67; static_assert at `config_schema.hpp:189`; cross-stack mirrors (`test_config_schema.cpp:24`, `test_config_apply.cpp` apply_get_all/apply_get_schema row counters bumped, plus webctl `test_config_schema_parity.py:37`). Full `[DEPRECATED]` warning block in `config.cpp:1064-1078` deleted. cold_writer comments updated to reference `grid.origin_yaw_deg` SSOT. SYSTEM_DESIGN.md §13.8 past-tense.
- B13 `/api/maps` response no longer carries the legacy `flat` key (backend `app.py:2148-2169`); frontend (`protocol.ts` + `stores/maps.ts`) derives flat list from `groups.flatMap(...)`; coupled change ships together.
- B10 subsumed by B12 (the schema row got deleted entirely).

**Mode-B finding M1 (non-blocking, queued as issue#28.2)**: B2/B3/B4 SSE tests pin only the broadcaster *relay* (frame inject → frame relay), not the *producer* side (`/api/map/edit/coord` handler emitting frames with proper shape). A regression that drops `request_id` from server-side publish, or returns "rejected" without `reason`, would NOT be caught. Indirect coverage exists via the existing `test_app_integration.py` Apply pipeline tests and B1 (real two-concurrent-POST). A future ~30-min add: one integration test that subscribes to `/sse/map-edit/progress`, fires `/api/map/edit/coord`, and asserts the actual emitted sequence carries `request_id` matching the response body.

**Open queue for next session** (priority order, operator-locked):

1. **issue#11 — Live pipelined-parallel multi-thread** (RESUMED — empirical CPU thrashing motivator now in hand). First action: open `.claude/memory/project_live_mode_cpu_thrashing_2026-05-05.md` + `.claude/tmp/plan_issue_11_live_pipelined_parallel.md` + `cat /proc/cmdline` on news-pi01 to confirm `isolcpus=` kernel parameter status. Round 2 Mode-A on the plan.
2. **issue#28.2 — SSE producer-side end-to-end pin** (NEW carryover from PR #93 Mode-B M1). ~30-min scoped follow-up; clean warm-up before tackling issue#11.
3. **issue#26 — measurement tool round 2 + Writer + HIL** (was paused; may now be unblocked from issue#11's gating).
4. issue#13 distance-weighted likelihood; issue#4 silent-converge diagnostic; issue#29 SHOTOKU base-move; Bug B Live standstill jitter; issue#7 boom-arm angle masking.

### 2026-05-05 (morning — 10:30 KST → 13:00 KST, twenty-fifth-session — issue#30.1 / #32 / #33 cleanup batch ships; HIL surfaces three pre-existing bugs adjacent to the issue#30 fold)

Twenty-fifth-session opened immediately off the twenty-fourth-session close (PR #86 docs at `6f6ce6d` 10:13 KST → main). Operator brief: TL;DR #1 (issue#30.1 — Mode-B round 2 backlog) and TL;DR #2 (issue#28.1 — B-MAPEDIT-3 follow-ups) were the queued priorities. Operator chose option 1 (issue#30.1 first, smaller batch with PR #84 context still fresh). Net result: **3 merged PRs in 2.5 hours** — the planned issue#30.1 cleanup, PLUS two emergent bug fixes (issue#32 + issue#33) discovered during operator HIL of the prior PR. issue#28.1 deferred to next session per operator scope decision.

**Notable structural revelation #1 — `_TS_REGEX` two-layer drift**. `map_backup._TS_REGEX = r"^[0-9]{8}T[0-9]{6}Z?$"` (the actual FS-touching regex) had been updated for the post-PR-#83 KST timestamp transition, accepting BOTH legacy `Z`-suffix and new no-suffix forms. But the sister regex `app._BACKUP_TS_PATTERN = r"^[0-9]{8}T[0-9]{6}Z$"` (the FastAPI Path validator, the FIRST defence layer that runs BEFORE the handler) was not updated. Result: every backup created on/after 2026-05-04 returned 422 at the FastAPI layer BEFORE `restore_backup` could even run. Operators saw "복원 실패" on every recent backup. Lesson: when relaxing/transitioning a regex/format, audit ALL parser sites for the same format — defence-in-depth layers can drift independently. (Memory candidate for Parent — pattern adjacent to but distinct from `feedback_relaxed_validator_strict_installer.md`.)

**Notable structural revelation #2 — Apply-on-pristine lineage chain invariant silently violated**. `app.py:801` initialized `parent_lineage: list[str] = []` and only populated it inside the `if active_name != pristine_base and active_sidecar_path.is_file():` branch (the derived case). Result: when the operator applied directly on a pristine map (the FIRST derivative case — by far the most common), the resulting derived sidecar wrote `lineage.generation = 0` + `lineage.parents = []` despite the source clearly being the pristine. Subsequent edits on the chain mis-counted generation by 1 because each `parent_sc.lineage_parents + [active_name]` step started from the wrong baseline. The lineage-chain invariant `len(parents) == generation` was implied by the design but never test-pinned. The operator caught it by reading `<LineageModal>` content during PR #87 HIL — "Generation: 0 / Parents: (none) / Cumulative: (-1.723, 0.521) m" — and recognising the contradiction. Fix: initialize `parent_lineage = [pristine_base]`. New pytest pin makes the invariant explicit going forward.

**Notable structural revelation #3 — manual maps_dir snapshot is a complementary safety net to SPA per-pair backup**. Operator-locked the rule mid-session (during issue#30.1 HIL prep): "앞으로도 간간히 SPA의 백업 기능 말고 이렇게 수동 백업을 하는 것이 좋겠어요". The SPA's Backup tab takes per-pair snapshots at operator request, which is great for routine ops but doesn't capture the entire `maps_dir` shape (orphan PGMs, unmatched sidecars, in-flight derivatives) atomically. A wholesale `rsync -a /var/lib/godo/maps/ /home/ncenter/maps_backup_<YYYY-MM-DD>_<context>/` gives a known-good rollback target distinct from the per-pair feature. Two snapshots taken this session as proof: `maps_backup_2026-05-05_tue_pre_issue30.1_hil/` (71 files) and `maps_backup_2026-05-05_pre_issue33_hil/` (38 files; reduced count reflects HIL-time cleanup). Memory entry committed in PR #88: `feedback_manual_maps_backup_pre_hil.md` with the trigger condition (PR touches `maps_dir`-mutating code) + naming convention + verify step.

**Process choice — pipeline short-circuit applied to issue#32**. Per `feedback_pipeline_short_circuit.md` (≤200 LOC fully-SSOT-specified work), issue#32 ran direct-writer (no Planner / no Reviewer Mode-A or Mode-B) — the regex change was 1 character (`Z$` → `Z?$`), the frontend formatter was a 16-LOC pure function with clear contract, and the column-header swap was trivial. Total surface: 9 files / +246 / -22. Operator HIL pass + merge in under 30 minutes. Confirmed third application of the rule (Phase 4-2 C, Phase 4-2 systemd, now issue#32). issue#33 was similarly small in code (~5 LOC core fix) but had two coupled changes (lineage init + backup map-name display) so direct-writer pipeline was retained without explicit short-circuit framing.

**3 PRs landed this session**:

| PR | Issue | Title | State |
|---|---|---|---|
| #87 | issue#30.1 | chore(issue#30.1): PR #84 Mode-B round 2 backlog — tautological test removal + inline lineage glyph + LineageModal a11y fix | merged `837d3f2` (squash, 5 commits → 1) |
| #88 | issue#32 | fix(issue#32): backup TS regex + frontend formatter accept post-PR #83 KST stamps | merged `6ad4112` (squash, 2 commits → 1) |
| #89 | issue#33 | fix(issue#33): Apply-on-pristine lineage init + backup table 맵 이름 column | merged `af5b1bc` (squash, 1 commit → 1) |

PR #87 internal commits (squashed):
1. `8734b43` — remove tautological D3↔D4 cross-check test (sister coverage at `test_typed_delta_shifts_picked_point_off_origin` pins the contract)
2. `b8a9cf9` — emit `lineage_kind` on `/api/maps` rows (additive `MapEntry` field; `_read_lineage_kind` graceful-degradation helper)
3. `070ccbb` — inline lineage glyph in `<MapList>` + `<LineageModal>` a11y split (DOM split for `role="dialog"` to live off the click/keydown-handler-bearing backdrop)
4. `a9075f6` — per-stack CODEBASE change-log entries
5. `e9fae2b` — Mode-B fold: 4th `LINEAGE_GLYPHS` entry for `backup` value (Mode-B caught webctl SSOT had 4 lineage_kind values but inline mapping covered only 3)

PR #88 internal commits (squashed):
1. `6d9e04f` — docs: capture manual maps_dir snapshot convention (carryover memory from issue#30.1 session)
2. `d4fb109` — fix(issue#32): backup TS regex + frontend formatter accept post-PR #83 KST stamps

PR #89 (single commit, merged as-is via squash):
1. `b8c4688` — fix(issue#33): Apply-on-pristine lineage init + backup table 맵 이름 column

**Test counts**:

- godo-webctl: 1049 (start) → 1051 (end) — net +2 (new: `test_backup_restore_kst_stamp_no_z_suffix_returns_200`, `test_apply_on_pristine_writes_generation_1_with_pristine_in_parents`); test_sidecar.py dropped 1 (tautological deletion), test_maps.py added 2 (lineage_kind sidecar present + malformed graceful-None), test_app_integration.py extended 1 existing.
- godo-frontend: 464 (start, post-PR #84) → 479 (end) — net +15 (new: `MapList.test.ts` 3 cases incl. backup glyph; `LineageModal.test.ts` 1 case; `format.test.ts` 11 cases incl. `backupTsToUnix` 4 + `backupMapNames` 5 + 2 sanity).
- All builds clean of LineageModal a11y warnings (PR #87 outcome).

**Open queue for next session** (priority order, operator-locked):

1. **issue#28.1** — B-MAPEDIT-3 follow-ups (half-day batch carryover from twenty-second-session): MA1 (4 missing pytests around asyncio.Lock + SSE protocol), MA2 (smoke→integration test promotion), MA3-9 (docstring fixes, unused constant removal, HIL tests, kRadToDeg promotion), Cleanup (standalone overlay component file deletion, `cfg.amcl_origin_yaw_deg` hard-removal, `flat` backward-compat key removal).
2. issue#26 — measurement tool round 2 + Writer + HIL (half-day at broadcasting-room wired LAN).
3. issue#11 — Live pipelined-parallel (PAUSED awaiting issue#26 first capture).
4. issue#13 — distance-weighted AMCL likelihood.
5. issue#4 — AMCL silent-converge diagnostic.
6. issue#29 — SHOTOKU base-move (issue#30 dependency now resolved).
7. issue#17 — GPIO UART migration (perma-deferred).
8. Bug B — Live mode standstill jitter.
9. issue#7 — boom-arm angle masking (contingent on issue#4).

**Next free issue integer: `issue#34`** (`#30` MERGED, `#30.1` MERGED, `#31` candidate = vector map perma-defer, `#32` MERGED, `#33` MERGED).

### 2026-05-05 (cross-day — 02:00 KST → 12:00 KST, twenty-third + twenty-fourth-session — issue#30 pick-anchored YAML normalization ships with 3 HIL fold rounds, including driver-fix migration from sed-patch to `flip_x_axis` runtime parameter)

Twenty-third-session opened 02:00-ish KST, ran the issue#30 pipeline to completion (Planner R1→R3 + Mode-A R1→R3 APPROVE + Writer R1→R2 + Mode-B R1→R2 APPROVE WITH MINOR), pushed commit `640b420` to PR #84, deployed to news-pi01 for HIL. Operator HIL surfaced 3 findings; operator chose to defer all to twenty-fourth-session for clean coupled fix rather than partial fix that "더 꼬일 수도". Twenty-third-session closed with PR #85 (handoff: NEXT_SESSION rewrite + new memory `project_issue30_hil_findings_2026-05-05.md`) merged at `6973167`.

Twenty-fourth-session opened 06:30 KST, root-caused all 3 findings, applied 3 fold rounds, merged PR #84 at `af3b6cf`. Net: 1 merged PR over the cross-day arc (4 commits squashed), 1 docs handoff PR (#85), and a complex 3-round Finding 2 (driver) journey that taught a deep ROS-driver-internals lesson.

**Notable structural revelation #1 — SLAMTEC RPLIDAR `M_PI - angle` driver formula vs datasheet**. Slamtec/rplidar_ros's upstream `src/rplidar_node.cpp:247-251` converts raw RPLIDAR angles to ROS `/scan` via `M_PI - angle`. Per SLAMTEC's own datasheet (Figure 2-4 page 11) the correct REP-103 conversion is `ψ = -θ` (single negation handles both left-handed→right-handed handedness flip and CW→CCW direction). The driver's `M_PI - θ = -θ + π` adds an extra 180° rotation on top, mapping every beam endpoint to the origin-symmetric position. HIL `test_180check_left_obstacle` PGM fingerprint: 1m wall physically forward placed at PGM `-x_world`. World-span asymmetry (`x ∈ [-1.15, +10.25]` for a wall-in-front setup) confirmed the point-reflection. Memory: `project_rplidar_cw_vs_ros_ccw.md` rewritten as full 5-path SSOT.

**Notable structural revelation #2 — bbox/affine sign mismatch causes silent canvas cropping**. Issue#30's `map_transform.py` had `_off_center_bbox` using `R(-θ)` for forward corner placement and `_affine_matrix_for_pivot_rotation` using `R(+θ)` matrix coefficients (output→input). For Pillow's output→input convention, these two pieces are mathematical inverses ONLY when their parameters compose correctly. Round-1 Finding-1 fix (`helper(-theta_rad)`) made BOTH use `R(-θ)` — same matrix instead of inverses — causing observed canvas-cropping on `05.05_v3.20260505-085323-after_fix` (bottom wall clipped). Final fix (revert helper to `helper(+theta_rad)`) restored bbox/affine as inverses AND aligned the visual rotation direction with the operator-locked semantic. Memory: `project_pick_anchored_yaml_normalization_locked.md` adds explicit "Yaw rotation sign convention" section.

**Notable structural revelation #3 — non-conventional angle range silently breaks rf2o scan-to-scan registration**. Round-2 driver fix sed-patched `M_PI - angle` → `-angle` in `rplidar_node.cpp`. Single-frame orientation became correct (verified by `test_180check_after_fix`), but cumulative mapping over operator motion broke: walls would ghost / not be re-recognised across consecutive scans. Operator HIL: "지도가 잔상처럼 남아있어 … 움직이면 계속 새로 그리는 듯해." Diagnosed as the sed shifting the published `/scan` `angle_min/angle_max` from conventional `[-π, π]` to `[-2π, 0]`, which degraded rf2o's internal scan alignment math. Replaced with `flip_x_axis: True` runtime parameter (existing in upstream driver), which composes with `M_PI - angle` to produce identity in physical correspondence AND keeps the conventional `[-π, π]` range. Composition explanation: two 180° rotations (one in published angle values, one in array index ordering via `apply_index += n/2`) compose to identity in beam-position correspondence. Memory: same `project_rplidar_cw_vs_ros_ccw.md` SSOT updated.

**Notable structural revelation #4 — operator yaw direction lock matches math convention, not PR #81 direction**. Original Q2 lock semantic was "typed +θ = world frame +θ CCW → bitmap visually rotates -θ (CW)". This matched PR #81's `Image.rotate(-typed_yaw_deg)` direction. Operator HIL on the round-1 Finding-1 fix discovered this direction was opposite to their intuition — they kept typing negative values to get visual CCW rotation. Operator-locked the math-convention direction explicitly: "typed +θ = bitmap visually rotates +θ CCW" (positive angle = CCW = standard math convention). Frontend `twoClickToYawDeg` co-fixed to negate atan2 output for the new lock. Memory: `project_pick_anchored_yaml_normalization_locked.md` "Yaw rotation sign convention" section.

**Process violation + lesson — fix v1 ship before mode-B re-review broke production cumulative mapping**. Round-1 Finding-1 fix shipped after webctl tests passed (1047/1047 green). Operator HIL on the deployed fix discovered (a) yaw direction wrong vs intuition AND (b) canvas-cropping AND (c) cumulative-mapping ghosting. None of these were caught by tests — direction-vs-intuition needs human judgment, cropping needs HIL with picked-pivot off-center, ghosting needs cumulative motion. Lesson: **structural fixes that change scan/coordinate semantics MUST go through operator HIL across BOTH single-frame AND cumulative mapping** before being considered shippable. Test pass alone is insufficient. (Memory candidate for Parent.)

**2 PRs landed this session** (cross-day arc):

| PR | Issue | Title | State |
|---|---|---|---|
| #84 | issue#30 | feat(issue#30): pick-anchored YAML normalization + sidecar SSOT | merged `af3b6cf` (squash, 4 commits → 1; with 3 HIL fold rounds) |
| #85 | n/a (handoff) | parent: NEXT_SESSION.md rewrite + issue#30 HIL findings memory | merged `6973167` (squash, twenty-third-session close docs) |

PR #84's 4 internal commits (squashed):
1. `640b420` — original feat (twenty-third-session pipeline output)
2. `e865aed` — HIL fold v1 (driver sed + AFFINE -theta + recovery_sweep guard + cleanup)
3. `b668f26` — operator-relock yaw direction + frontend 2-point negate + cropping fix
4. `f9694e4` — driver fix v2 (sed → `flip_x_axis: True` runtime parameter)

**Open queue for next session** (priority order, operator-locked):

1. issue#30.1 — Mode-B round 2 backlog (half-day): tautological test rewrite, MapList lineage glyph inline, LineageModal a11y warning.
2. issue#28.1 — B-MAPEDIT-3 follow-ups (half-day batch from PR #81 Mode-B Major findings).
3. issue#26 — measurement tool round 2 + Writer + HIL (half-day at broadcasting-room wired LAN).
4. issue#11 — Live pipelined-parallel (PAUSED awaiting issue#26 first capture).
5. issue#13 — distance-weighted AMCL likelihood.
6. issue#4 — AMCL silent-converge diagnostic.
7. issue#29 — SHOTOKU base-move (issue#30 dependency now resolved).
8. issue#17 — GPIO UART migration (perma-deferred).
9. Bug B — Live mode standstill jitter.
10. issue#7 — boom-arm angle masking (contingent on issue#4).

### 2026-05-04 (morning → late-night — 06:00 KST → 22:30 KST, twenty-second-session — issue#28 ships B-MAPEDIT-3 yaw rotation full stack + 5 HIL fix rounds)

Twenty-second-session opened directly off the twenty-first-session close (PR #80 docs merged 06:11 KST → main = `046d49d`). Operator brief: "B-MAPEDIT-3 yaw rotation 다음 세션 1순위" plus mid-session "이번에 같은 PR로 맞추자" — fold every Critical / Major / cascade item into ONE PR rather than splitting follow-ups. Net result: 1 merged PR (#81 squash at `da78dd0`), 54 files / +6746 / -384 LOC, 1456/1456 tests pass, longest pipeline stack of any session to date (Planner → Mode-A ACCEPT-WITH-NITS → Writer × 3 commits → Mode-B ACCEPT-WITH-NITS → Writer × 1 fold-in → operator HIL × 5 rounds → 5 HIL-fix commits → squash-merge).

**Notable structural revelation — `grid.origin_yaw_deg` is NOT used by AMCL likelihood-field cell mapping**. The YAML `origin[2]` SSOT this PR introduced is consumed by tracker in only TWO places: `apply_yaw_tripwire` (informational diagnostic) and `compute_offset` (subtracted from pose.yaw to produce the FreeD output offset). `evaluate_scan` in `scan_ops.cpp` ignores it — the AMCL particle filter operates as if origin_yaw = 0, with the YAML yaw acting only as an OUTPUT-stage offset. This was discovered during the operator's PICK#1/PICK#2 cascade analysis and explains why **Option B (bake-into-bitmap) is the only correct path** for yaw correction: rotating the bitmap shifts the likelihood field's content, which AMCL DOES see; updating YAML yaw alone (Option A) would change the output `dyaw` but not the pose math, so a wall at world angle X stays at X regardless of YAML yaw. Memory candidate (flagged for Parent).

**Process violation + lesson #1 — components shipped but never mounted**. Round 2 HIL: "Map 목록이 SPA에 보이지 않음" + "axis가 화면 테두리에 박힘". Root causes: (a) `<MapList>` component created in Part 1 but never mounted on Map.svelte (legacy `<MapListPanel>` still rendering with stale `MapEntry[]` consumption against the new `{groups, flat}` response shape — frontend silently rendered nothing); (b) standalone `<OriginAxisOverlay>` + `<GridOverlay>` canvases used PGM logical dimensions and didn't subscribe to `viewport.zoom`/`panX/Y`, so pan/zoom by the underlay didn't reach them; (c) `OriginAxisOverlay` `yamlOriginX/Y` bound to `currentOrigin[0..1]` instead of `(0, 0)` so axis intersection landed at canvas pixel (0, H). Lesson: **"NEW component shipped" ≠ "wired feature"** — Mode-B should pin a HIL-style "did you mount it on a route?" check. Memory candidate.

**Process violation + lesson #2 — yaw direction sign mismatch (round 4 HIL)**. `map_rotate.py:153-154` docstring stated "rotate by `-typed_yaw_deg`" but line 217 passed `+typed_yaw_deg` to Pillow's `Image.rotate()`. Off-by-sign from the module's own documented intent. Coupled with: app.py's `_apply_map_edit_pipeline` passed `theta_deg` through to `apply_origin_edit_in_memory` which SUBTRACT'd YAML yaw, AND map_rotate physically rotated the bitmap — double-counting. Combined effect: a wall at world angle X ended up at world angle X − 2θ instead of 0. Operator caught it on first end-to-end Apply: "Yaw 의도와 반대로 돌아가고 있어." Memory candidate: docstring-vs-implementation drift detection during Mode-B.

**1 PR landed this session**:

| PR | Issue | Title | State |
|---|---|---|---|
| #81 | issue#28 | B-MAPEDIT-3 yaw rotation — full plumbing + pristine/derived map model + segmented Edit UI | merged `da78dd0` |

Sub-commit timeline within PR #81 (10 commits before squash):

1. `9b49aba` — Part 1: tracker plumbing (cold_writer reads `grid.origin_yaw_deg`, cfg field deprecated with stderr warning) + webctl backend (`maps.py`, `map_rotate.py` NEW, `map_origin.py` extension, `sse.py` NEW, `/api/map/edit/{coord,erase,progress}` endpoints, `/api/maps` grouped tree) + frontend NEW components (`EditModeSwitcher`, `OverlayToggleRow`, `GridOverlay`, `OriginAxisOverlay`, `ApplyMemoModal`, `MapList`) + `overlayToggles` store + `originMath.ts` extensions + tests. ~1850 LOC.
2. `1a5c514` — Part 2 SPA wire-up: `OriginPicker.svelte` restructured (THETA_EDIT_ENABLED=true, 2-click yaw state machine, dirty flag split) + `MapEdit.svelte` `<EditModeSwitcher>` mounted with per-mode Apply/Discard + Map.svelte mounts + C7 split (math contract pin renamed to `mapOverlayCoherenceMath.test.ts`).
3. `2491324` — clangd unused-include cleanup in `test_occupancy_grid.cpp`.
4. `3fe0a39` — Mode-B CR1+CR2+CR3 + MA10: real `<OriginAxisOverlay>` + `<GridOverlay>` component-mount tests, `ColdWriterTest.OffsetComputedAgainstGridYaw_NotConfigField` asymmetric pin (cfg=0°, grid=10°), SPA filters SSE frames by `request_id`, SYSTEM_DESIGN §13 "Map edit pipeline" + FRONT_DESIGN §3 I3-bis added, 2 NEW memory files (`project_pristine_baseline_pattern.md`, `feedback_overlay_toggle_unification.md`).
5. `24ecf22` — Latent TypeError fix: server now emits `derived_pair: {pgm, yaml}` + `pristine_pair` shapes the SPA's success banner already expected (TS interface declared them but server never sent — first Apply would have thrown `Cannot read properties of undefined (reading 'pgm')`).
6. `fdffdc5` — HIL round 1 P0 fixes: (a) `stores/maps.refresh()` now extracts `flat` from `{groups, flat}` response shape (legacy `<MapListPanel>` was iterating wrapper object → empty list); (b) `<OriginAxisOverlay>` `yamlOriginX/Y={0}` so axis intersects world (0, 0) inside the bitmap (was at canvas corner); (c) `coordClickMode` default `'off'` → `'xy'` so canvas is immediately clickable in Coord mode.
7. `0d0a86e` — HIL round 2 fix: mount `<MapList>` grouped tree on `Map.svelte` above legacy `<MapListPanel>` + `mapGroups` writable in `stores/maps.ts` + `<ConfirmDialog>` activate/delete wrappers + `OriginPicker.svelte` `inlineApplyEnabled={false}` from MapEdit (operator was clicking the legacy `/api/map/origin` Apply button next to the numeric inputs instead of the new modal-flow button at the top toolbar).
8. `87af3bc` — HIL round 3+4 fix: (a) `map_rotate.py:217` `img.rotate(typed_yaw_deg)` → `img.rotate(-typed_yaw_deg)` per module docstring intent; (b) `app.py:_apply_map_edit_pipeline` passes `theta_deg=None` to `apply_origin_edit_in_memory` to stop YAML yaw double-counting against bitmap rotation. Pinned by `test_rotation_direction_negates_typed_yaw` (90° rotation lands dark cell to right of center) + `test_post_map_edit_coord_with_theta_does_not_double_count` (derived YAML origin[2] = pristine origin[2]).
9. `917cefa` — HIL round 4 UI fix: NEW `lib/overlayDraw.ts` with `drawOriginAxis` / `drawGrid` / `drawPickPreview` pure helpers called inside MapUnderlay's `ondraw` hook. Removes standalone overlay canvas mounts on both Map.svelte and MapEdit.svelte. Overlays now inherit viewport pan/zoom transform from MapUnderlay's `worldToCanvas` projector. Adds orange pick preview state (`xyPreview`, `yawP1Preview`, `yawP2Preview`) cleared on Apply / Discard.
10. `853864d` — HIL round 5 fix: grid extends with `yawDeg` param so lines align with the rotated axis overlay. `drawGrid` inverse-rotates visible world bounds into the local rotated frame, generates line endpoints in local coords, forward-rotates back to world for projection.

**Operator-locked decisions this session** (all tracked in spec memory `project_map_edit_origin_rotation.md`):

- **Pristine baseline pattern** — `<base>.{pgm,yaml}` immutable; every Apply emits `<base>.YYYYMMDD-HHMMSS-<memo>.{pgm,yaml}` derived pair. Quality loss = 1× resample regardless of iteration count.
- **Translate first, then rotate** composition order.
- **Lanczos-3 + re-threshold** intent (Pillow constraint forced fallback to BICUBIC; documented in code comments as a separate-pipeline follow-up).
- **Auto-expand canvas** for long studios.
- **No-cancel atomic Apply** with tracker control disable.
- **SUBTRACT semantic for yaw** (mirror of x/y per `feedback_subtract_semantic_locked.md`); ROTATE#1/#2 fixtures pinned (`5°→10°→−5°`, `−5°→20°→−25°`).
- **Segmented-control Edit tab** (Coordinate / Erase) with per-mode Apply/Discard; mode switch does NOT auto-discard.
- **Two independent picks** within Coordinate mode (origin OR yaw OR both).
- **Two-click yaw pick** — P1→P2 vector defines +x; length ignored.
- **Dual-input mandate** — GUI + numeric (+/- 0.01 step) coexist.
- **Postfix memo regex** `[A-Za-z0-9_-]+`.
- **Hybrid grouped tree map list** (pristine parent, derived indented children, active badge, manual confirm).
- **Manual active map switch + confirm dialog**, no auto-restart.
- **Tracker plumbing fix**: `cfg.amcl_origin_yaw_deg` → YAML `origin[2]` SSOT (with 2-step deprecation: keep field as ignored + stderr warning one release, hard-remove in follow-up).
- **Unified overlay toggle row** (Origin/Axis, LiDAR migrated, Grid) with localStorage persistence.
- **Origin/Axis overlay** = origin dot + +x red + +y green axes to screen edge (REP-103); intersects WORLD (0, 0).
- **Grid overlay** = world-frame anchored, zoom-adaptive, **rotates with `yamlYawDeg` to match the axis** (round-5 HIL-locked).
- **Coherent rendering** Vitest pin (pose+scan+bitmap rotate together).
- **Orange pick preview**: filled dot for XY pick, hollow ring for yaw P1-only, arrow for P1+P2.

**Operator deferral** (operator-locked 2026-05-04 KST late-night): "이번에 같은 PR로 맞추자" closed the round, BUT the **YAML normalization to (0, 0, 0°)** path (operator's "Option Q" from the PICK#1/PICK#2 cascade analysis) is queued for a NEW PR in twenty-third-session. Current SUBTRACT-from-pristine semantic produces `new_yaml = pristine - typed`, NEVER (0, 0, 0°); operator's mental model wants the new derived map's YAML to read (0, 0, 0°) so the picked point IS the new world origin by definition. This requires bitmap translation + canvas adjustment + cumulative-typed tracking, NOT just metadata change. Spec lives in operator's PICK#1/PICK#2 cascade analysis — Parent will absorb into a new memory entry for next-session Planner brief.

**Live system on news-pi01 (post twenty-second-session close)**:

- `godo-tracker.service`: rebuilt from `da78dd0`. `cold_writer` now reads `grid.origin_yaw_deg()` (YAML SSOT); `cfg.amcl_origin_yaw_deg` retained as deprecated field with one-shot stderr warning. Tier-2 schema row still parseable (no TOML break per `feedback_toml_branch_compat.md`).
- `godo-webctl.service`: NEW endpoints live — `POST /api/map/edit/coord` (JSON), `POST /api/map/edit/erase` (multipart), `GET /api/map/edit/progress` (SSE), `GET /api/maps` returns `{groups, flat}` grouped tree shape.
- `godo-frontend` (`/opt/godo-frontend/dist/`): mounts `<EditModeSwitcher>` segmented control, `<OverlayToggleRow>` unified toggle, `<MapList>` grouped tree, `<ApplyMemoModal>` SSE-driven progress, viewport-tracking overlays via `lib/overlayDraw.ts`.
- HIL pre-deploy step (operator-executed): `sudo sed -i.bak '/^amcl\.origin_yaw_deg/d' /var/lib/godo/tracker.toml` to silence the deprecation warning.

UNCHANGED: godo-irq-pin, godo-cp210x-recover, godo-mapping@active, polkit (14 rules), Docker `godo-mapping:dev`, `/etc/udev/rules.d/99-rplidar.rules`, `/run/godo/`, journald.

**Open queue for next session** (priority order, operator-locked):

1. **issue#30 — YAML normalization (0, 0, 0°) per Apply** (NEW, operator-locked twenty-second-session late-night) — opens new PR. Bitmap translation + canvas adjustment + cumulative-typed tracking from pristine. Spec source: PICK#1/PICK#2 cascade analysis from this session. Uncovers the gap that current SUBTRACT-from-pristine + bake-into-bitmap can never produce YAML = (0, 0, 0°).
2. **issue#28.1 — B-MAPEDIT-3 follow-ups from Mode-B Major findings** (MA1 4 missing pytests, MA2 test relocation, MA3 docstring fix, MA4 unused LANCZOS_FILTER_NAME, MA6 overlay projection at extreme zoom, MA7 webctl synchronous-vs-202 doc note, MA8 promote `kRadToDeg` to `core/constants.hpp`, MA9 schema row DEPRECATED label) + standalone `<OriginAxisOverlay>` + `<GridOverlay>` component file deletion (no longer mounted).
3. **issue#26 — cross-device latency measurement tool** (PAUSED at Mode-A round 1 REJECT). Round 2 + Writer + HIL is half-day at broadcasting-room.
4. **issue#11 — Live pipelined-parallel multi-thread** (PAUSED awaiting issue#26 first capture).
5. **issue#13 (continued) — distance-weighted AMCL likelihood**.
6. **issue#4 — AMCL silent-converge diagnostic**.
7. **issue#29 — SHOTOKU base-move + two-point cal-reset workflow** (depends on issue#30 normalization for clean re-anchor math).
8. **issue#17 — GPIO UART** (perma-deferred).
9. **Bug B — Live mode standstill jitter** (analysis-first).
10. **issue#7 — boom-arm angle masking** (contingent on issue#4).

