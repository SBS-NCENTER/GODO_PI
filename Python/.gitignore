# UV / venv
.venv/
.uv-cache/

# Python bytecode
__pycache__/
*.py[cod]
*.egg-info/

# Test / build artifacts
.pytest_cache/
.mypy_cache/
.ruff_cache/
dist/
build/

# Captured data, session logs, and test-session outputs are intentionally
# tracked in git so collaborators can review results. If a specific capture
# should stay local (e.g. contains sensitive content), add an explicit rule
# for it here.

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
