# godo-frontend/CODEBASE/ — Weekly archives

ISO 8601 weeks (Mon–Sun KST). Each archive holds the dated entries from
its calendar week. The master file `../CODEBASE.md` keeps the live
invariants/scaffold and the Index of weekly archives. Rotation rule: at
session-close, the previous "most recent" entry in the master rotates
into its matching weekly file.
