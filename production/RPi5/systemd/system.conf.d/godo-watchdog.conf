# /etc/systemd/system.conf.d/godo-watchdog.conf
# Enables the BCM2712 hardware watchdog: systemd PID 1 pets the watchdog
# at half this interval; if PID 1 hangs, the kernel resets the system.
[Manager]
RuntimeWatchdogSec=10s
